<div class="form-new">
  <%= simple_form_for([list, bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.association :movies, input_html: { data: { controller: "tom-select" } }%>
    <%= f.submit 'Add Movie', class: 'main-button'%>
  <% end %>
</div>
